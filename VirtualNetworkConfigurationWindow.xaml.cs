using System.Windows;

namespace TheNetworkProject
{
    public partial class VirtualNetworkConfigurationWindow : Window
    {
        public VirtualNetworkConfigurationWindow()
        {
            InitializeComponent();
        }

        // Pour l'instant, on peut ajouter une simple logique pour renvoyer un DialogResult.
        // Par exemple, fermer la fenêtre avec DialogResult = true si les paramètres sont validés.
    }
}